Recommended next steps (in order)

1. ~~Scaffold the Next.js app from §1 — App Router groups, features/* shells, auth stub, empty dashboard layout.~~ **Done**
2. ~~Lock the data model — Prisma schema for Household, CreditCard, PromoPeriod, Statement, Payment (+ intents), UserPreference.~~ **Done (Prisma + SQLite)**
3. ~~Ship vertical slice #1 — Sign in → onboarding (manual card) → /cards table → /overview metric grid.~~ **Done**
4. ~~Ship vertical slice #2 — Paydown priority + promo payoff panels (pure calc on manual data).~~ **Done**
5. Ship vertical slice #3 — Payment runway + one-click into /payments/new intent flow.
6. Then statements, search, reminders/jobs — still MVP, but after the core loop works.

>Defer: aggregator/Plaid, in-house autopay, rewards swipe matrix, landing marketing scope.

## How to run

```bash
pnpm install
pnpm db:push
pnpm db:seed
pnpm dev
```

Sign in as Marti → Overview (metrics + paydown + promo plan) + Cards table.
